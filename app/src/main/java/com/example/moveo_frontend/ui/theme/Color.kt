package com.example.moveo_frontend.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──────────────────────────────────────────────────────────────────
val BluePrimary  = Color(0xFF0047AB)
val BlueAccent   = Color(0xFF3B82F6)
val BlueLight    = Color(0xFFE0EAFE)

// ── Semantic ────────────────────────────────────────────────────────────────
val GreenSuccess  = Color(0xFF27AE60)
val RedAlert      = Color(0xFFE74C3C)
val OrangeReward  = Color(0xFFF59E0B)

// ── Neutral ─────────────────────────────────────────────────────────────────
val GrayBackground = Color(0xFFF6F6F6)   // fondo del sistema
val GraySurface    = Color(0xFFFFFFFF)   // tarjetas / superficies
val GrayUI         = Color(0xFFE4E6E9)   // botones neutros, inputs
val GrayBorder     = Color(0xFFE5E7EB)   // bordes sutiles

// ── Text ─────────────────────────────────────────────────────────────────────
val TextDark       = Color(0xFF000000)               // texto principal
val TextMuted      = Color(0xFF000000).copy(alpha = 0.62f)  // texto secundario / 62%
val TextLight      = Color(0xFFF6F6F6)               // texto sobre fondos oscuros
val NavInactiveColor = Color(0x6B0A0A0A)             // íconos nav inactivos
